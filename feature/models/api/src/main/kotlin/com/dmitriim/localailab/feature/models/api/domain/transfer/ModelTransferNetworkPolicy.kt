package com.dmitriim.localailab.feature.models.api.domain.transfer

/** Network access permitted for one explicitly requested model transfer. */
enum class ModelTransferNetworkPolicy {
    WIFI_ONLY,
    ANY_NETWORK,
}
